# Raw county data — contains buyer/seller names; not published.
data/

# All generated per-city outputs — caches, parcels, maps, exports.
# Rebuildable via src/geocode.py / src/fetch_parcels.py and the notebooks.
# (The published map is copied to index.html at the repo root, which is tracked.)
output/

# Python / Jupyter cruft
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
